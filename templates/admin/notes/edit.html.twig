{% extends 'base.admin.html.twig' %}

{% block content %}
<h2>Modifier la note</h2>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-3">Enregistrer</button>
    <a href="{{ path('admin_notes') }}" class="btn btn-secondary mt-3">Retour</a>
{{ form_end(form) }}
{% endblock %}